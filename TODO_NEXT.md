# 다음 할일 (타이머 관련)

## 일별 뷰
1. ✅(완료) 일별 뷰에서 오늘이 아닐 때만 "오늘로 이동" 버튼 노출 (주/월은 항상)

## 타이머 팝업 / 입력
2. ✅(완료) 타이머 팝업 시/분/초 인풋박스 수정
   - 패딩 조정으로 숫자 보이게 (좌4/우16, 박스 폭 90)
   - 화살표 버튼 박스(테두리/배경) 제거 → 삼각형만
   - 삼각형 색상 hover 시 회색→보라 변동(기존 유지)

## 타이머 버튼 상태
3. ✅(완료) 정지 중 셀 아이콘을 ‖(붉은색)로 표시 (호버 시엔 ▶'재개' 안내 유지)
10. ✅(완료) 닫기로 타이머 안 멈춤: ✕는 패널 상시표시만 끄고 타이머는 계속 진행
    (말풍선 내리면 캐릭터 옆 타이머 풍선으로 계속 보임)

## 상시 타이머 기능 (신규)
4. ✅(완료) 상시 타이머 기능 — 단일 슬롯, 기존 타이머 패널 확장 방식
6. ✅(완료) 타이머 패널 상시 출력 + 캐릭터 우클릭 "타이머 패널 표시" on/off (기본 OFF)
7. ✅(완료) 할일 없이 idle 패널의 ▶ + 시간 + −/+(1분 단위)로 일반 타이머 설정·시작
   - 첨부 이미지 참고 (재생 ▶ + 4:00 + −/+)
   - 기존 패널 확장으로 구현(시계 다이얼로그 재설계는 안 함)
9. ✅(완료) 일반 타이머 ↺ 초기화 → 직전 설정 시간으로 (패널 헤더 ↺ 버튼, 일반 타이머일 때만 노출)

## 레이아웃
5. ✅(완료) 패널 폭 120→140, 타이머 셀 108×108 정사각형(고정), 타이머 블록 고정 195 · 밀린할일이 나머지 채움

## 타이머 정지 시 버튼
8. ✅(완료) 할일 타이머 정지 시 셀 아래 완료·초기화 버튼 2개 노출
   - 완료=대상 할일 완료 처리+타이머 해제 / 초기화=그 타이머 초기 설정 시간으로
   - 일반(상시) 타이머 정지 시엔 미노출(거긴 헤더 ↺ 사용)

## 애니메이션
11. ✅(완료) 투두 조회 애니메이션 방향 변경: 좌우가 아니라 세로로 (페이드 교체)
    - on: 아래에서 위로
    - off: 반대로 위에서 아래로

## 캐릭터 이미지
12. ✅(완료) 목록 열림/닫힘 각각 캐릭터 이미지 설정 (설정>이미지 탭 '목록 열림'/'목록 닫힘', 미설정 시 기본 폴백)
13. ✅(완료) 일→주→월 전환 시 새 뷰 영역 페이드 인(180ms) — 폭 변화는 즉시(추후 튜닝 가능)
14. ✅(완료) 할일 완료 및 타이머 완료 시 기본 이미지로 전환되는 시간을 3초로 연장

## 설정 메뉴
15. ✅(완료) 설정 메뉴 정리
    - '타이머 완료 시 할일 자동완료' 항목 삭제 (UI만 제거, 기능/설정키는 유지)
    - '로그인 시 자동 시작' → '부팅 시 자동 시작'으로 텍스트 수정
16. ❌(보류) 설정 창 저장/취소/적용 — 모달 구조라 미리보기 이점 없고 항목별 revert 비용 큼
